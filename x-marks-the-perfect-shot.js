// create a function that takes coordinated directions in the form of 
// an array of strings
// directions will move us in a [x,y] grid of 1x1
// the output will return an array representing our final location in a coordinate of [x,y]

const moves = ['north', 'north', 'west', 'west', 'north', 'east', 'north']

const finalPosition = function (moves) {
  const position = [0, 0];
  

  return finalCoord;
}


